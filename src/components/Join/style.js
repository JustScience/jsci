import styled from 'styled-components'

const JoinWrap = styled.section`
    padding: 24px;
    width: 100%;
`
const FormWrap = styled.form`
`
const FormField = styled.div`
    background-color: #bada55;
    margin-bottom: 4px;
    padding: 8px;
`

export { JoinWrap, FormWrap, FormField }